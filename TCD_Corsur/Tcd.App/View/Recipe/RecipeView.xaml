<UserControl x:Class="Tcd.App.RecipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tcd.App"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:RecipeViewModel}">
    <Grid Margin="12" Background="{StaticResource PanelBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Recipes">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Margin="8,8,8,4" FontSize="11"
                           Text="{Binding RecipesDirectory}"/>
                <ListBox Margin="8" ItemsSource="{Binding RecipeNames}" SelectedItem="{Binding SelectedRecipeName}" />
            </DockPanel>
        </GroupBox>

        <GroupBox Grid.Column="2" Header="Editor">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,10,6" Text="Name"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,6" Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,10,6" Text="U"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,6" Text="{Binding U, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,10,6" Text="V"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,6" Text="{Binding V, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,10,6" Text="W"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,6" Text="{Binding W, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,10,6" Text="Z Load"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ZLoad, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,10,6" Text="Z Bond"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ZBond, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,0,10,6" Text="Robot Stage/Upper/Lower"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" Margin="0,0,0,6" Text="Fixed (Stage / UpperLoad / LowerLoad)"/>

                    <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,0,10,0" Text="Notes"/>
                    <TextBlock Grid.Row="7" Grid.Column="1" TextWrapping="Wrap"
                               Text="현재는 단순 버전으로 축(U/V/W/Z) 티칭만 JSON으로 저장합니다. 로봇은 enum teaching으로 확장 가능합니다."/>
                </Grid>
            </ScrollViewer>
        </GroupBox>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Width="110" Margin="0,0,8,0" Content="Reload" Command="{Binding Cmd_Reload}" />
            <Button Width="110" Margin="0,0,8,0" Content="New" Command="{Binding Cmd_New}" />
            <Button Width="110" Margin="0,0,8,0" Content="Save" Command="{Binding Cmd_Save}" />
            <Button Width="110" Margin="0,0,8,0" Content="Save As..." Command="{Binding Cmd_SaveAs}" />
            <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" Text="{Binding Status}" />
        </StackPanel>
    </Grid>
</UserControl>

