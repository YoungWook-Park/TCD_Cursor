<UserControl x:Class="Tcd.App.View.Manual_RobotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Tcd.App"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:Manual_RobotViewModel}">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <GroupBox Header="Robot (Teach Move)">
      <StackPanel Margin="10">
        <TextBlock Margin="0,0,0,8"
                   Style="{StaticResource HighlightTextBlockStyle}"
                   Text="Move robot to teach positions (from recipe)"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
          <Button Width="120" Margin="0,0,8,0" Content="Move Stage"
                  Style="{StaticResource PrimaryButtonStyle}"
                  Command="{Binding Cmd_RobotMoveStage}" />
          <Button Width="120" Margin="0,0,8,0" Content="Move UpperLoad"
                  Style="{StaticResource PrimaryButtonStyle}"
                  Command="{Binding Cmd_RobotMoveUpper}" />
          <Button Width="120" Content="Move LowerLoad"
                  Style="{StaticResource PrimaryButtonStyle}"
                  Command="{Binding Cmd_RobotMoveLower}" />
        </StackPanel>
        <TextBlock Text="{Binding Status}" />
      </StackPanel>
    </GroupBox>
  </Grid>
</UserControl>

